{
  "name": "gymapp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:server": "node --experimental-sqlite --watch server/server.js",
    "dev:full": "concurrently -n vite,api -c cyan,yellow \"vite\" \"node --experimental-sqlite --watch server/server.js\"",
    "seed": "node --experimental-sqlite server/seed.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "concurrently": "^8.2.2",
    "vite": "^5.0.0"
  }
}
